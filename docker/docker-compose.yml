version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - 5433:5432  # expose postgres port to be able to access it from the host machine, but use 5433 to avoid collisions
  
  py:
    image: python:3
    tty: true # do not exit immediately
    depends_on:
      - db